// src/types.ts (or types.d.ts)

export interface Player {
  id: number;
  name: string;
  imagepath: string;
  totalgoals: number;
  totalassists: number;
  currentclub: string;
  age: number;
  country: string;
}
